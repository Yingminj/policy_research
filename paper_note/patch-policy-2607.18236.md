# Patch Policy：用稠密视觉表征做高效具身控制

> 阅读笔记，撰写于 2026-08-09。内容基于 arXiv 摘要页、项目主页与 arXiv HTML 全文渲染抽取；表格数字已尽量逐条还原，但**未逐一比对官方 PDF**，引用前建议复核关键数值。

---

## Metadata

- **Title**: Patch Policy: Efficient Embodied Control via Dense Visual Representations
- **Authors**: Gaoyue Zhou, Zichen Jeff Cui, Ada Langford, Bowen Tan, Yann LeCun, Lerrel Pinto
- **Affiliations**: NYU Courant Institute；Meta-FAIR；AMI Labs（LeCun、Pinto 有双重署名）
- **Venue / year**: arXiv preprint, 2026-07-20（v1）；cs.RO, cs.LG；未标注投稿会议
- **Links**:
  - paper: https://arxiv.org/abs/2607.18236v1
  - project: https://patch-policy.github.io/
  - code: https://github.com/gaoyuezhou/patch_policy
- **Tags**: robot learning, behavior cloning, visual representation, ViT, patch token, block-causal attention, VQ-BeT, Diffusion Policy, VLA, Franka manipulation

```bibtex
@misc{zhou2026patchpolicyefficientembodied,
      title={Patch Policy: Efficient Embodied Control via Dense Visual Representations},
      author={Gaoyue Zhou and Zichen Jeff Cui and Ada Langford and Bowen Tan and Yann LeCun and Lerrel Pinto},
      year={2026},
      eprint={2607.18236},
      archivePrefix={arXiv},
      primaryClass={cs.RO}
}
```

---

## 一句话总结

**（机器人操作 / 模仿学习）** 本文提出 Patch Policy——一个对 transformer 策略的极小改动：用 **block-causal attention mask** 让策略直接吞下冻结预训练 ViT 的稠密 patch token（帧内双向、帧间因果），从而在不引入十亿参数 VLM 的前提下保留细粒度空间信息；在 4 个仿真 + 3 个真机任务套件上相对全局池化表征取得约 **40% 的相对提升**，并以约 **0.7%（ViT-S）～4.4%（ViT-L）的参数量** 超过微调后的 OpenVLA-OFT 约 **18%**。

---

## 研究背景与动机

### 领域现状

视觉模仿学习的策略架构目前分成三条互不兼容的路线：

1. **全局表征路线**：每帧图像被压成一个 token（CLS token、average pooling，或 DynaMo 这类用动力学目标学出来的全局表征），再喂给 VQ-BeT / Diffusion Policy 之类的序列策略。优点是序列短、训练快、控制频率高。
2. **从零训练的视觉主干**：ACT 用从头训练的 ResNet-18 patch 特征，保留了空间维度，但完全放弃了大规模视觉预训练的红利。
3. **VLA 路线**：OpenVLA-OFT 这类模型确实在 patch 级别操作（DINOv2 + SigLIP patch），但代价是继承整个 VLM 主干——7.61B 参数、61.71 ms 推理延迟。

同时，视觉表征学习本身进展很快：DINOv2 / DINOv3 / WebSSL / V-JEPA 2 / SigLIP 2 每年都在刷新稠密特征质量。机器人社区却基本只能通过"全局池化"这个窄口子去消费它们。

### 现有痛点

- **信息瓶颈**：把 256 个 patch 压成 1 个 token，丢掉的正是精细操作最需要的空间细节（谁在哪、离目标差几毫米）。
- **红利浪费**：从零训练的主干拿不到预训练收益；全局池化拿到了预训练权重却扔掉了它最值钱的部分。
- **成本错配**：唯一能吃稠密特征的现成方案（VLA）把"稠密视觉"和"十亿参数语言模型"绑死了，而高频反应式控制根本不需要后者。
- **升级路径缺失**：每出一个更好的视觉编码器，机器人策略无法"即插即用"地受益。

### 核心矛盾

> **控制精度需要稠密空间表征，但现有的稠密表征消费方式（VLA）把训练/推理成本抬到了高频控制承受不起的量级；而承受得起的方式（全局池化）恰好扔掉了稠密性。**

作者认为，这个 trade-off 其实是**架构惯性**造成的假两难，而不是本质约束。

### 本文目标

不是提出新的视觉编码器，也不是提出新的动作头，而是提出**中间那一层缺失的连接件**：让任意标准 transformer 策略能够以可承受的代价直接消费任意冻结预训练 ViT 的 patch token。成功标准明确：

- 显著超过同策略头 + 全局表征的版本；
- 匹配或超过 patch 级基线（ACT、OpenVLA-OFT）；
- 保持训练小时数与推理延迟在高频控制可用范围内；
- 换编码器不用改架构。

### 切入角度

**架构驱动 + 表征驱动**，但刻意做到最小：不改视觉编码器（全程冻结）、不改动作头（VQ-BeT 与 Diffusion Policy 都能挂）、只改 **attention mask 与序列布局**。

### 核心 idea

标准策略之所以要把每帧压成一个 token，是因为序列策略默认用 token 级因果掩码——一旦把 256 个 patch 展平进序列，token 级因果会让同一帧内靠前的 patch 看不到靠后的 patch，破坏了空间的整体性（图像内部本来就不存在"时序"）。

作者的观察是：**时间上需要因果，空间上不需要**。于是把掩码改成分块的——同一帧的所有 patch 之间完全双向可见，跨帧则严格因果。这样一次改动同时满足了"帧内做完整空间推理"和"不泄漏未来观测"两个约束，稠密 token 就可以直接进序列，不需要任何压缩，也不需要 VLM。

---

## 方法详解

### 整体框架

数据流：

```
o_t ∈ R^{C×H×W}
   ↓ 冻结 ViT 编码器（DINOv2 / WebSSL / …）
   patch 特征 P × D        （例：224×224 输入 → 16×16 网格 = 256 patches；DINOv2 ViT-S: D=384；WebSSL: D=1024）
   ↓ 展平时空 patch 序列 + 学习式位置嵌入（+ 本体状态 token、goal token）
   ↓ Transformer trunk（block-causal mask）
   ↓ 动作头：VQ-BeT 或 Diffusion Policy
   动作 / 动作块
```

- **训练时更新**：只有 transformer trunk + 动作头。视觉编码器**全程冻结**，不做端到端微调。
- **目标条件**：图像目标用同一编码器编码后与观测拼接；向量目标拼到每个观测 token 上。

### 关键设计

**1. Block-causal attention mask（核心，也是唯一的真正新增件）**

- *解决什么*：稠密 token 进序列后，token 级因果掩码会人为地在图像内部强加一个不存在的顺序。
- *怎么做*：patch 在帧内完全双向注意，跨帧因果掩码。
- *为什么比直觉方案好*：直觉方案有两个——全注意力（full attention）会泄漏未来观测，破坏策略的因果性；token 级因果会切碎空间结构。block-causal 恰好是两者的正确折中。
- *代价与假设*：序列长度从 T 涨到 T×P（论文里 P=256），注意力是平方复杂度，训练时间明显上升——这是作者自己承认的主要成本。

**2. 冻结编码器 + 可换插槽**

- 论文横向测了 5 个编码器（DINOv2 / DINOv3 / WebSSL / V-JEPA 2 / SigLIP 2），架构不变只换权重。这既是消融，也是论文主张的"管道"价值证明：视觉表征社区的进展可以直接被吸收。
- 代价：不微调意味着遇到特殊视觉域（作者点名的 limitation）时没有适配手段。

**3. 动作头无关（head-agnostic）**

- VQ-BeT 与 Diffusion Policy 都能挂，且两者都受益。这一点让"Patch Policy 是一个改动而非一个模型"的定位更站得住。

### 训练与实现设置

| 项目 | 设置 |
|---|---|
| 输入分辨率 | 224×224 |
| patch 网格 | 16×16 = 256 patches（DINOv2 ViT-S: 384 维；WebSSL: 1024 维） |
| 观测历史长度 | 任务相关（Push-T: 5 帧；LIBERO Goal: 10 帧；BlockPush: 3 帧） |
| 动作块大小 | 任务相关（Push-T: 5；LIBERO Goal: 1；BlockPush: 1） |
| trunk 规模 | VQ-BeT 典型配置 8 层 / 8 头 / 512 维；DP 8 层 / 4 头 / 256–512 维 |
| 学习率 | 1e-4 ~ 5e-5 |
| batch size | 32–256 |
| 优化器 / 训练步数 | 论文未在抽取到的部分明确说明（只给了收敛小时数） |
| 演示数据量 | Push-T 206 条；LIBERO Goal 500 条（50/任务）；BlockPush 1000 条；Cube 550 条 |

---

## 实验关键数据

### 环境与指标

| 环境 | 类型 | 指标含义 | 量程 |
|---|---|---|---|
| Push-T | 仿真 | target coverage 覆盖比例 | 0–1 |
| LIBERO Goal | 仿真 | success rate | 0–1 |
| BlockPush | 仿真 | 正确放置的方块**平均个数** | 0–2 |
| Cube | 仿真 | 到达目标位的方块**平均个数** | 0–2 |
| Cable Insertion / Pen Collection / Tool Hanging | 真机（7-DoF Franka） | 分阶段成功率，20 次试验 | 0–1 |

### 主实验（Table 1，仿真）

全局池化基线 vs Patch Policy（同一策略头对比）：

| 表征 | 策略头 | Push-T | LIBERO Goal | BlockPush | Cube |
|---|---|---|---|---|---|
| DynaMo（全局） | VQ-BeT | 0.66 | 0.93 | 0.65 | 0.28 |
| WebSSL Avg Pool | VQ-BeT | 0.54 ±0.02 | **0.97** ±0.04 | 0.84 ±0.18 | 0.25 ±0.02 |
| WebSSL CLS | VQ-BeT | 0.59 ±0.01 | 0.95 ±0.01 | 0.77 ±0.08 | 0.23 ±0.01 |
| **WebSSL Patch（本文）** | VQ-BeT | 0.68 ±0.03 | 0.94 ±0.01 | **1.68** ±0.15 | **1.68** ±0.03 |
| DynaMo（全局） | Diffusion Policy | 0.73 | 0.68 | 1.06 ±0.10 | 0.27 |
| WebSSL Avg Pool | Diffusion Policy | 0.79 ±0.02 | 0.98 ±0.01 | 1.34 ±0.02 | 0.21 ±0.03 |
| WebSSL CLS | Diffusion Policy | 0.68 ±0.02 | **0.99** ±0.01 | 0.99 ±0.12 | 0.21 ±0.03 |
| **WebSSL Patch（本文）** | Diffusion Policy | **0.80** ±0.01 | 0.98 ±0.00 | 1.65 ±0.08 | **1.73** ±0.02 |

patch 级基线：

| 表征 | 策略 | Push-T | LIBERO Goal | BlockPush | Cube |
|---|---|---|---|---|---|
| DINOv2+SigLIP Patch | OpenVLA-OFT | 0.59 ±0.02 | 0.95 | 1.43 ±0.17 | 1.50 ±0.09 |
| ResNet-18 Patch（从零） | ACT | 0.64 ±0.03 | 0.93 ±0.02 | 0.15 ±0.01 | 0.69 ±0.11 |

**⚠️ 我的判断：增益高度集中，"40%" 是被两个环境拉起来的。** 逐环境算 VQ-BeT patch vs WebSSL avg pool 的相对提升：Push-T +26%、LIBERO **−3%**、BlockPush +100%、Cube **+572%**。Diffusion Policy 侧更极端：Push-T +1.3%、LIBERO 0%、BlockPush +23%、Cube +724%。也就是说 **LIBERO Goal 上稠密表征几乎没有收益（甚至略负），Push-T 上在 DP 头下也基本打平**；论文的整体优势几乎完全来自 BlockPush 和 Cube 这两个多物体、需要精确空间定位的任务。这与论文自己的叙事（稠密特征对精细/多目标操作关键）是一致的，但摘要里那句"40% relative improvement"给人的印象是全面均匀提升，**实际是场景依赖的**。引用这个数字时应加限定。

### 真机实验（Table 2，20 次试验，分阶段成功率）

| 方法 | Cable Insertion（完全插入） | Pen Collection（放入第三支） | Tool Hanging（挂上） |
|---|---|---|---|
| **DINOv2 Patch VQ-BeT（本文）** | **0.70** | **0.85** | **0.90** |
| DINOv2 CLS VQ-BeT | 0.60 | 0.65 | 0.70 |
| OpenVLA-OFT | 0.30 | 0.60 | 0.65 |
| ResNet-18 Patch ACT | 0.35 | 0.65 | 0.85 |

真机任务规格：Cable Insertion 需 ~2 mm 容差、101 条演示（~37 min）；Pen Collection 三支笔、初始位姿随机 ±1 cm / ±15°、52 条演示（~45 min）；Tool Hanging 50 条演示（~18 min）。

真机上优势比仿真更一致（三项全胜），且在**容差最紧的 Cable Insertion 上差距最大**（0.70 vs 次优 0.60，vs OpenVLA-OFT 0.30）。这是全文最有说服力的一组证据——精度需求越高，稠密表征收益越大。

### 计算效率（Table 3）

| 方法 | 总参数 | 可训练参数 | 推理延迟 (ms) | 等效频率 |
|---|---:|---:|---:|---:|
| VQ-BeT ResNet-18 | 39.95M | 28.77M | 5.79 | ~173 Hz |
| ACT | 83.85M | 83.85M | 8.63 | ~116 Hz |
| **本文 VQ-BeT + DINOv2** | 51.55M | 29.49M | 10.99 | ~91 Hz |
| **本文 VQ-BeT + WebSSL** | 334.00M | 30.34M | 21.43 | ~47 Hz |
| OpenVLA-OFT | 7.61B | 177.90M | 61.71 | ~16 Hz |

训练成本：本文 DINOv2 版本 **6.5 GPU-hours**（1×L40S, 6.5h）；OpenVLA-OFT **16 GPU-hours**（4×L40S, 4h）。

**⚠️ 摘要中的 "0.7% of parameters" 需要读细则**：0.7% 对应 ViT-S（51.55M / 7.61B = 0.68%），而拿下 18% 提升的主力配置是 WebSSL（334M = 4.4%）。论文正文自己写的是"beats OpenVLA-OFT with under 5% of its parameters with ViT-L (as little as ~0.7% with ViT-S)"，措辞是准确的；**摘要把两个配置的最优数字并置了**，读起来偏乐观。

### 消融 1：空间压缩（Table 4, Push-T）

| patch 数 | 性能 |
|---:|---:|
| 256 | 0.69 |
| 64 | 0.52 |
| 16 | 0.53 |
| 4 | 0.51 |
| 1 | 0.48 |

*隔离了什么*：把稠密特征在空间上降采样，其余全部不变。
*发生了什么*：**从 256 掉到 64 就损失了绝大部分收益（0.69 → 0.52），之后到 1 只再掉 0.04**。
*意味着什么*：性能不是随分辨率平滑下降的，而是有一个**陡崖**——只要开始池化就基本回到全局表征水平。这是全文最重要的单个结果：它说明"稠密"不是一个可以线性折中的旋钮。

### 消融 2：注意力掩码（Table 12, Push-T & Cube）

| 掩码 | Push-T VQ-BeT | Cube VQ-BeT | Push-T DP | Cube DP |
|---|---:|---:|---:|---:|
| Full Attention | 0.64 | 1.09 | 0.83 | 1.10 |
| Token-Causal | **0.73** | 1.36 | 0.70 | **0.11** |
| **Block-Causal（本文）** | 0.70 | **1.38** | **0.83** | **1.24** |

*隔离了什么*：只换掩码。
*发生了什么*：block-causal **并非每格最优**——Push-T VQ-BeT 上 token-causal 更高（0.73 vs 0.70），Push-T DP 上与 full attention 打平（均 0.83）。但 token-causal 在 Cube + DP 上**灾难性崩溃到 0.11**。
*意味着什么*：block-causal 的价值是**跨任务、跨动作头的稳健性**，而不是峰值性能。这个诚实的结论比"我们的掩码全面最优"更可信，但也说明核心贡献的收益边界比摘要暗示的窄。

### 消融 3：编码器横评（Table 7, VQ-BeT）

| 编码器 | Push-T | LIBERO Goal | BlockPush | Cube |
|---|---:|---:|---:|---:|
| DINOv2 | **0.69** ±0.01 | **0.96** ±0.01 | 1.20 ±0.23 | 1.35 ±0.03 |
| WebSSL | 0.68 ±0.02 | 0.94 ±0.01 | **1.68** ±0.15 | **1.68** ±0.02 |
| V-JEPA 2 | 0.65 ±0.01 | 0.86 ±0.03 | 1.46 ±0.13 | 1.36 ±0.03 |
| SigLIP 2 | 0.51 ±0.01 | 0.83 ±0.01 | 0.99 ±0.10 | 1.17 ±0.03 |

（DINOv3 亦在测试之列，抽取到的表格未列出其数值。）
自监督表征（DINOv2 / WebSSL）明显优于图文对比表征（SigLIP 2）——**语言对齐的特征对控制不利**，这一点与"VLA 的 VLM 主干未必是必需品"的主张互相印证。

### 消融 4：模型规模（Table 13, Push-T + DINOv2）

| 层数, 头数, 维度 | 参数量 | VQ-BeT coverage |
|---|---:|---:|
| 4, 4, 64 | 25.62M | 0.50 |
| 6, 6, 120 | 26.64M | 0.57 |
| 8, 8, 512 | 51.55M | 0.69 |

性能随 trunk 容量单调上升，且**未见饱和**——只测了 3 个点，最大 51.55M，尚不能判断天花板在哪。

### 零样本泛化（Table 5 / 6）

| 设置 | 基线 (CAP) | 本文 |
|---|---:|---:|
| 真机零样本抓取 | 79% | **87%** |
| EgoGym real-to-sim: Pickup | 75.78% | **79.50%** |
| EgoGym real-to-sim: Open | 67.88% | **71.40%** |
| EgoGym real-to-sim: Close | 86.50% | **92.44%** |

（"CAP" 基线的全称与设置在我抽取到的内容中未明确说明，需查原文。）提升幅度温和（+3~8 个百分点），属于加分项而非核心论据。

---

## 关键发现

- **稠密性有陡崖，不是旋钮**：256 → 64 patch 丢掉 0.17，64 → 1 只再丢 0.04（Table 4）。任何形式的空间池化几乎立刻把你打回全局表征的水平。**这是本文最可迁移的经验结论。**
- **收益与任务的空间精度需求强相关**：LIBERO Goal 上几乎零收益，Cube 上 +572%，真机 Cable Insertion（2 mm 容差）上差距最大。稠密表征不是普适加成，是**精度税的减免**。
- **稠密 ≠ 昂贵**：OpenVLA-OFT 证明的是"VLM 很贵"，不是"patch token 很贵"。冻结 ViT + 30M 可训练参数就能拿到 patch 的好处，延迟 11–21 ms（47–91 Hz），落在反应式控制可用区间。
- **自监督 > 图文对齐**：SigLIP 2 在四个环境上全面垫底（Table 7）。对控制而言，语言监督引入的语义抽象反而损失了几何信息。
- **block-causal 买的是稳健而非峰值**：单点上 token-causal 有时更好，但会在特定"任务×动作头"组合上崩溃（Cube+DP: 0.11）。

---

## 亮点与洞察

**可借鉴的**：

1. **"最小连接件"式的问题定位**。作者没有造新编码器、新动作头、新数据集，而是识别出两个成熟组件之间缺失的接口。这种论文的价值密度往往很高，且容易被采用——只要 mask 逻辑几十行。
2. **head-agnostic 的验证方式**。同时挂 VQ-BeT 和 Diffusion Policy，并且两者都受益，是"这是一个架构性质而非调参运气"的强证据。值得在自己的工作里模仿这种验证结构。
3. **压缩阶梯消融（Table 4）的设计**。用 256/64/16/4/1 这条阶梯把"稠密到底有多重要"变成一条可读的曲线，比"有 patch vs 无 patch"的二元对比信息量大得多，并且直接暴露了陡崖位置。
4. **把"可换插槽"当成贡献来验证**。5 个编码器横评不只是消融，而是在为论文的核心主张（"社区可以持续吸收视觉表征进展"）提供证据。

**惊讶的经验模式**：全局池化基线在 Cube 上普遍只有 0.21–0.28（≈ 2 个方块里搞定 0.25 个），几乎是完全失败；而 patch 版本能到 1.68–1.73。这个断崖式差距大到需要警惕（见下）。

---

## 局限与展望

### 作者承认的局限

1. 只用**冻结**视觉主干，未探索端到端微调以适配特殊视觉域。
2. 稠密 token **拉长序列、增加训练时间**；FlashAttention 等优化尚未应用。
3. 仅作为**行为克隆**策略评估；未扩展到 RL，因而受限于专家演示的性能天花板。

### 读者应警惕的局限（我的判断）

1. **"40%" 的聚合方式不透明**。抽取到的正文只说"aggregates results across all tested environments"，没有给出聚合公式。考虑到 Cube 上的相对提升高达 +572%，任何"先算相对提升再平均"的做法都会被这一项主导。**这个数字应被视为宣传性摘要指标，不应直接复用。**
2. **全局基线在 Cube 上是否被合理调参？** 0.21–0.28 的表现接近完全失败。当基线在某个环境上崩到地板，通常有两种解释：任务确实需要稠密空间信息（作者的解释），或基线在该环境上没有被认真调优。论文未给出基线的超参搜索说明，这一点无法从外部判断。
3. **摘要的参数量口径混用**（0.7% 对应 ViT-S，18% 提升对应 WebSSL 的 4.4%）。正文准确，摘要乐观。
4. **序列长度的实际代价没有被量化**。推理延迟给了，但**训练时间随 P 的增长曲线没给**——只给了单点的 6.5 GPU-hours。对想上更高分辨率或更长历史的人来说，这是最需要的一条曲线，恰好缺失。
5. **注意力是 O((T·P)²)**。T=10、P=256 时序列长 2560，再加多相机就更长。真机三任务用的是单一/少量相机；**多视角设置下的可扩展性未被验证**，而这恰是真实操作平台的常态。
6. **模型规模只测了 3 个点**，且未饱和，无法判断本文报告的数字距离该架构的上限有多远。
7. **真机 20 次试验的统计功效很弱**。0.70 vs 0.60（Cable Insertion）在 n=20 下差异约 2 次成功，落在噪声范围内；真正显著的是 vs OpenVLA-OFT 的 0.70 vs 0.30。
8. **CAP 基线未在抽取内容中定义**，零样本部分的可比性无法从外部核实。

### 可做的下一步

- 补 **P（patch 数）× 训练时长 × 性能** 的三维曲线，并加上 FlashAttention/线性注意力后重测——这是本文留下的最大空白。
- 在帧内做**内容感知的 token 选择/合并**（而非均匀池化）：Table 4 证明均匀降采样会掉崖，但没有排除"选对 32 个 patch"能保住性能的可能。这是最直接的后续课题。
- 多相机/多视角下的序列长度治理。
- 端到端微调 vs 冻结的对比（作者自己点名）。
- 扩展到 RL 或残差策略，突破 BC 天花板。

---

## 相关工作与定位

| 轴 | ACT | OpenVLA-OFT | DynaMo / CLS / AvgPool | **Patch Policy** |
|---|---|---|---|---|
| 视觉主干 | ResNet-18，从零训练 | DINOv2+SigLIP，随 VLM 微调 | 预训练 ViT，但只取全局 token | 预训练 ViT，**冻结** |
| 输入粒度 | patch | patch | 单 token / 帧 | patch（256/帧） |
| 参数量 | 83.85M | 7.61B | ~40M | 51.55M（ViT-S）/ 334M（WebSSL） |
| 延迟 | 8.63 ms | 61.71 ms | ~5.79 ms | 10.99 / 21.43 ms |
| 预训练红利 | ✗ | ✓（含语言） | 部分（被池化损耗） | ✓（纯视觉，无损耗） |
| 换编码器 | 需重训 | 绑定 VLM | 可换但收益受限 | **即插即用** |

- **vs ACT**：同样吃 patch，但 ACT 放弃了预训练——BlockPush 上 0.15 vs 1.68 的巨大差距说明"patch 结构"本身不够，**patch + 预训练**才是关键组合。
- **vs OpenVLA-OFT**：本文的核心论点就是解耦——你想要的是稠密视觉 token，不是语言模型。Table 7 中 SigLIP 2 垫底进一步支持了这个解耦。
- **vs 全局表征系**：Table 4 的压缩阶梯直接量化了这条路线损失了什么。

---

## 与当前研究方向的关联

（针对本目录下 `literature-review.md` 关注的"图像质量 → 动作质量"问题）

这篇论文和你正在做的压缩敏感性研究有一处**直接且重要**的交集：

- 你的前期结论是 PSNR 与 DINO **CLS** 余弦相似度强相关（Spearman ρ = 0.927），并已指出"像素保真度 ≠ 策略成功率"是待补的缺口。
- Patch Policy 的 Table 4 提供了一个新的、更尖锐的角度：**CLS/全局特征本身就已经把策略最需要的信息丢掉了**。也就是说，用 CLS 余弦相似度作为压缩损伤的代理指标，可能系统性地**低估**了压缩对稠密特征的影响——因为 CLS 是全局池化的产物，对局部高频退化天然不敏感，而 JPEG/H.264 的损伤恰恰集中在高频细节上。
- 你的报告已经观察到腕部相机（wrist_L 38.99 dB vs 头部 45.95 dB）损伤最重。Patch Policy 的真机结果显示，**容差越紧的任务（Cable Insertion，2 mm）对稠密空间信息越敏感**——腕部相机 + 精细插入正好是这个交集。

**可操作的建议**：在现有 CLS 余弦相似度之外，增加一个 **patch 级**的退化指标（例如逐 patch 特征余弦相似度的分布，而非仅看均值；或 patch 特征的空间自相关变化）。如果 patch 级指标对 CRF/JPEG 质量的敏感度显著高于 CLS 级，那就直接证明了当前代理指标偏乐观——这本身就是一个可发表的结论，而且 Patch Policy 给了它一个现成的理论支点和一个现成的下游策略架构去验证。

---

## 评分

| 维度 | 分数 | 理由 |
|---|---|---|
| 新颖性 | ★★★☆☆ (3/5) | block-causal mask 在 NLP/多模态里不是新概念，创新在于**识别出它是机器人策略消费稠密特征的缺失接口**。属于"正确而非惊艳"的贡献。 |
| 实验充分度 | ★★★★☆ (4/5) | 4 仿真 + 3 真机 + 5 编码器横评 + 压缩阶梯 + 掩码消融 + 规模消融，覆盖面很好。扣分项：真机 n=20 功效不足；缺训练成本 vs 序列长度曲线；基线调参未说明。 |
| 写作质量 | ★★★★☆ (4/5) | 正文措辞谨慎（参数量口径写清了区间，掩码消融如实报告非最优格）；摘要偏乐观。**正文比摘要可信**。 |
| 复现/落地价值 | ★★★★★ (5/5) | 代码开源、改动极小、编码器可换、单卡 6.5 小时收敛、延迟落在高频控制区间。这是本文最强的一面——**几乎可以当天接入现有 pipeline**。 |
| 对当前研究的启发 | ★★★★☆ (4/5) | Table 4 的压缩陡崖结论对"压缩→策略性能"课题有直接方法论价值（见上一节）。 |

---

## 实用要点（TL;DR for 落地）

**值得直接借的**：
- block-causal mask + 冻结 ViT patch token，是当前性价比最高的策略视觉升级路径；改动小、可回退。
- 编码器优先选 **WebSSL 或 DINOv2**；避开 SigLIP 2 这类图文对齐表征。
- 别对 patch 特征做空间池化——Table 4 说明这基本等于放弃全部收益。

**需要小心的**：
- "40% 提升"是场景依赖的聚合数字，在 LIBERO 这类任务上可能为零。先在自己最关心的任务上验证，别按摘要预期规划。
- 序列长度 O((T·P)²) 的代价在多相机场景下未被验证，上多视角前先估算。
- 摘要中 0.7% 参数量与 18% 提升不是同一配置。

**这篇论文打开的问题**：
> 均匀空间池化会掉崖（Table 4），但**内容感知的 token 选择**是否能在 32–64 个 token 的预算下保住 256 token 的性能？如果能，稠密表征的成本问题就被解决了；如果不能，说明策略真正依赖的是完整空间场而非少数关键区域——这两种答案对机器人视觉的设计含义截然不同。

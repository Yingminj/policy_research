# VISTA：给 UMI 手持数据装上「感知对齐 + 物理可执行性打分」两道闸门

> 阅读笔记，撰写于 2026-08-17。
> 依据 arXiv v2（2026-06-04，v1 为 2026-06-03；v2 备注仅为 "Corrected the typing error"，论文页脚日期写 "May, 2026"）的 LaTeXML HTML 全文。
> 本笔记按用户要求，**重点展开 §3.3 的数据质量检测与轨迹评估指标**（见「轨迹打分框架」与「指标可信度审查」两节）。
> 未阅读官方代码（https://github.com/TeleHuman/umi-vista ），凡涉及实现细节处均标注为论文陈述而非代码核对结论。

---

## Metadata

- **Title**: VISTA: Vision-Grounded and Physics-Validated Adaptation of UMI data for VLA Training
- **Authors**: Siyuan Yang†, Linzheng Guo†, Ouyang Lu, Zhaxizhuoma, Daoran Zhang, Xinmiao Wang, Ting Xiao, Fangzheng Yan, Zhijun Chen, Yan Ding, Chao Yu, Chenjia Bai★, Xuelong Li（† 共同一作，★ 通讯：baicj@chinatelecom.cn）
- **Affiliations**: TeleAI（中国电信人工智能研究院）、Lumos Robotics、中科大、西工大、上交、华东理工、哈工程、复旦
- **Venue / year**: arXiv:2606.04708v2（2026-06，cs.RO / cs.AI），未见投稿会议信息
- **Links**:
  - paper: https://arxiv.org/abs/2606.04708
  - project: https://tele-umi-vista.github.io
  - code: https://github.com/TeleHuman/umi-vista
  - 数据/模型：论文承诺开源 physical-validation pipeline、UMI-VQA、验证后轨迹与预训练模型（本笔记未核实实际放出状态）
- **Tags**: VLA, UMI / FastUMI, 手持数据采集, 鱼眼腕视, 数据质量评估, 轨迹可执行性打分, cross-embodiment, flow matching, co-training, π0.5

---

## 一句话总结

针对「UMI 手持采集数据直接喂给大规模 VLA 效果不好」这一现象，本文把病因拆成**感知失配**（腕装约 180° 鱼眼视角对预训练 VLM 属于 OOD）与**物理失配**（人手采的轨迹不受目标机器人关节限位/自碰撞/控制器带宽约束），分别用 8M 规模的 UMI-VQA 鱼眼视觉语言数据与一套「连续性 / 自碰撞 / 执行保真度」三维轨迹打分流水线来治，再配两阶段共训；在 RoboTwin-UMI + LIBERO-UMI 上平均成功率 0.813（π0.5 为 0.758），20 个真机任务平均 0.598（π0.5 为 0.528）。

---

## 研究背景与动机

### 领域现状

UMI（Chi et al. 2024）与 FastUMI（Liu et al. 2024/2025）把「手持夹爪 + 腕装鱼眼相机 + 位姿跟踪」做成了可规模化的真机数据采集范式：不需要任何具体机器人在场，就能拿到第一视角图像 + 末端执行器轨迹 + 夹爪开合。后续 DexUMI、ActiveUMI、UMI-3D、RDT2 等在跟踪精度、数据规模、夹爪兼容性上继续扩展。这套数据训 Diffusion Policy / ACT 这类紧凑模仿学习策略效果已被反复验证。

### 现有痛点

作者的核心观察是：**UMI 数据对小策略好用，不等于对大 VLA 好用**，且社区普遍默认「UMI 数据是可以被任意下游学习器直接消费的」。这个默认在下游换成 VLA 时崩掉，原因有二：

1. **视觉分布**：主流 VLA 依赖 VLM backbone，其预训练语料与共训 VQA 都是标准针孔透视、全局构图；UMI 只有腕装鱼眼，强径向畸变、分辨率高度不均（中心细、边缘压缩）、夹爪与操作者手部造成严重自遮挡，且完全没有第三人称/主视角相机。
2. **物理可行性**：采集时人手完全不受目标本体约束——不受关节限位与可达工作空间约束，会产生跟踪跳变，会要求过高关节速度；采集端只跟踪夹爪位姿，机器人的肘/躯干/底座之间的自碰撞完全无人监督；还可能超出控制器带宽，这些问题往往到 replay 或部署时才暴露。

### 核心矛盾

UMI 之所以能规模化，正是因为它把数据采集从具体机器人本体上解绑；而 VLA 要可靠部署，恰恰需要监督信号与目标本体的运动学、碰撞几何、控制器能力一致。**「解绑带来的规模」与「部署需要的绑定」是同一枚硬币的两面。**

### 本文目标

不改 UMI 硬件范式的前提下，构造一条把 raw UMI 语料转成「VLA 可消费语料」的转换流水线，成功标准是：(a) 加入鱼眼对齐的 VQA 共训能稳定提升下游策略；(b) 物理打分能作为部署成功率的代理指标；(c) 同数据条件下整体系统超过 π0.5 / LingBot-VLA / Wall-X。

### 切入角度

纯**数据驱动 + 评测驱动**：不发明新架构（backbone 直接从 π0.5 初始化），而是造数据（UMI-VQA）与造数据筛选指标（物理打分）。我的判断是，这篇的真正贡献密度集中在 §3.3 的打分框架，而不是模型侧。

### 核心 idea

作者抓住的关键观察是：raw UMI 轨迹里「语义上正确、物理上不可执行」的样本占比不低，而 VLA 会把这种不可执行的动作模式当成技能一起内化，形成系统性部署失败。于是把「数据能不能用」重新定义为一个**关于目标本体 e 的条件函数** S(ξ, e)，而不是数据本身的固有属性——同一条轨迹对 RealMan 可能是废料，对 R1Pro 可能是良品。这直接推出两阶段筛选策略：预训练用宽松阈值跨多本体保多样性，下游微调对特定机器人收紧阈值。

---

## 方法详解

### 整体框架

```
raw UMI 数据
  ├── 图像流 ──> UMI-VQA 构造（真实鱼眼帧半自动标注 3M + RefSpatial 扩散编辑成鱼眼 5M）
  └── 轨迹流 ──> 数据完整性预检 ──> MuJoCo/Mink 跨本体 replay ──> 三维打分 S(ξ,e) ──> 阈值筛选
                                                                  ↓
                        Stage 1：VQA + FAST 离散动作 token 的自回归共训（backbone 可训）
                        Stage 2：冻结 backbone，训 flow matching 连续动作专家（知识隔离）
                                                                  ↓
                        下游：严格阈值筛该机器人数据 → 全模型解冻端到端微调 → Zenoh 分布式多机部署
```

- 输入：语言指令 l、左右腕鱼眼图像对 o_t、本体感受状态 s_t（末端位姿 + 夹爪宽度）
- 输出：长度 H 的动作 chunk a_{t:t+H-1}
- 采集硬件为 Lumos Robotics 的 FastUMI Pro（约 600 g）：中央主鱼眼（约 180° 对角 FOV）+ 两侧辅鱼眼 + 深度相机构成四目；位姿由 Vive Tracker（lighthouse）与板载视觉惯性 SLAM 双流融合，论文称亚厘米级（约 3 mm）；夹爪宽度由内置编码器读取。部署端在 RealMan / AC one / Galaxea R1 Pro 三个双臂平台上沿用**完全相同的腕相机配置与外参**，指纹几何也保持一致——这一点是「训练与部署视觉分布一致」的前提，也意味着该方案对相机改动很敏感。

### 关键设计一：UMI-VQA（感知对齐）

8M 样本，两个来源：

| 来源 | 规模 | 构造方式 |
|---|---|---|
| 真实腕装鱼眼帧 | 3M | 从真实 UMI 演示视频抽帧，用大 VLM（Qwen3VL 系）按任务化 prompt 生成结构化标注，再做「轻量人工校验」 |
| RefSpatial 改造 | 5M | 用 FLUX.2-dev 做语义感知的 image-to-image 编辑，把透视图转成鱼眼风格 |

真实部分按能力切成五个子集：Object Grounding（842K, 27.5%）、Interaction Grounding（894K, 29.1%）、Spatial Reasoning（824K, 26.9%）、Scene Understanding（406K, 13.2%）、Captioning（103K, 3.3%）。其中 Interaction Grounding 不标整个物体，而标**可交互区域**（抓取点、接触区、功能部件），这是相对 RefSpatial 类通用空间数据最有差异化的一块。

一个值得借鉴的判断：作者明确论证**不能用经典几何 warp** 把透视图转鱼眼——当目标 FOV 约 180° 超出原图视锥时，纯像素重映射只会产生边缘拉伸、外插失真或周边缺失，因为几何变换无法合成视锥外的场景结构；用扩散编辑模型则能「幻觉」出物理上说得通的周边内容。代价是引入生成幻觉，且论文没有报告生成图与真实鱼眼的分布距离或标注保真度。

### 关键设计二：轨迹打分框架（本文重点）

先做**数据完整性预检**，再对通过预检的轨迹沿三个维度打分。三个分数都归一化到 [0,100]。

**共用的三段式打分函数**（Eq. 2）。对一个通用位移量 d，超参 α、β：

- d ≤ d_min：满分 100（近乎理想的平滑度）
- d_min < d ≤ d_max：线性惩罚 100 − α·(d − d_min)/(d_max − d_min)
- d > d_max：指数衰减（论文文字说明此段对应硬件故障或跟踪丢失导致的严重不连续；HTML 版此处公式渲染残缺，指数项的确切形式需回 PDF 核对）

实用超参（论文给出的实际取值）：

| 分量 | d_min | d_max | d_scale | α | β |
|---|---|---|---|---|---|
| 平移 | 5 mm | 45 mm | 100 mm | 40 | 60 |
| 旋转 | 1° | 9° | 20° | 40 | 60 |

**(1) 轨迹连续性 s_tc（与本体无关）**：度量记录到的夹爪任务空间运动的固有平滑度。对相邻航点的位移 d^p_t（平移）与 d^r_t（旋转）分别打分，**每个时刻取两者中的较小值**，然后**对整条轨迹的所有相邻航点取最小值**作为 s_tc(ξ)。它评估的是 raw 轨迹本身，在任何机器人特定映射之前，因此可用于检测传感器丢帧、跟踪丢失、人手抽搐这类**采集质量问题**。

**(2) 自碰撞风险 s_sr（与本体相关）**：把轨迹在跨本体 replay 系统中重放，记录全时刻、全指定连杆对上的**最小碰撞对距离** d_col(ξ, e)，然后分段线性映射（Eq. 3）：≥ d_col,max 给满分；介于 min 与 max 之间线性插值；≤ d_col,min 直接给 0 分，对应硬自碰撞违规。**场景物体被显式排除**，以便把自碰撞与环境交互解耦——这是个干净的设计，但也意味着这套打分对「机器人撞桌子/撞被操作物」完全无感知。论文未报告 d_col,min / d_col,max 的具体数值。

**(3) 执行保真度 s_ef（与本体相关）**：同样在 replay 系统中重放，计算「演示末端位姿」与「机器人在关节级控制器下实际达到的位姿」之间的跟踪偏差 e_replay(ξ, e)。作者强调这个偏差是一个**聚合量**，同时吸收了接近关节限位、运动学奇异、工作空间边界等多种本体因素——换言之它是个黑箱式的综合可执行性代理，不做因果归因。位置误差与角度误差用与 s_tc 相同的函数形式分别打分，每时刻取较小者，全程取最小值。

**replay 基础设施**：MuJoCo 仿真 + Mink 库做运动学与 IK。流程为选定任务与机器人 → 机器人回 home → 在与 UMI 对齐的坐标系中规划一条过全部航点的平滑轨迹 → IK 转关节空间 → 下发到仿真或真机，打分在 replay 过程中采集。

**总分聚合（Eq. 4）——加权乘积模型**：

```
S(ξ,e) = 100 · (s_tc/100)^{w1} · (s_sr/100)^{w2} · (s_ef/100)^{w3},  w_i ≥ 0, Σw_i = 3
```

默认 w1=w2=w3=1。用乘积而非加权和，是为了**保留物理可行性的硬约束语义**：任一维接近 0 就整体接近 0，不能被另外两维的高分补偿。权重可按训练阶段调，例如微调时抬高 w2 偏好无碰撞轨迹。跨本体预训练时，按 N 个候选本体的平均兼容性 S_cross(ξ) = (1/N)Σ_e S(ξ,e) 排序，让「普遍可行」的演示优先进入通才策略。

**与既有做法的差异**：作者把 UMI / Wang et al. 2026 归为「硬过滤」——违反关节限位或动力学可行性就直接丢弃；VISTA 是「软验证」，连续打分 + 可调阈值，因而支持同一份数据在不同本体、不同训练阶段被差异化使用。这是本文最值得复用的抽象。

### 关键设计三：两阶段共训

- **Stage 1**：从 π0.5 checkpoint 初始化，在 8M UMI-VQA + 100K 通过宽松阈值验证的真机轨迹上做自回归共训。动作 chunk 经 FAST 离散化成 token z_{1:N_a}，VQA 答案 token u_{1:N_q}，两者用**同一个 next-token 预测目标**（Eq. 5–6，按目标 token 数 |y| 归一化的 NLL）。
- **Stage 2**：为避免灾难性遗忘，采用 Driess et al. 2026 的**知识隔离**策略——**冻结 VLM backbone**，只在其上训一个独立的连续动作专家 f_φ，目标为 flow matching：a_τ = (1−τ)ε + τa，L_fm = E‖f_φ(a_τ, τ, h_θ(o_t,l,s_t)) − (a − ε)‖²。
- **下游微调**：先用**严格阈值**按目标机器人筛数据，然后**全模型解冻**端到端更新 backbone 与动作专家。
- **部署**：纯 Python 分布式架构，Zenoh 作中间件，多臂状态汇聚到分布式 GPU 节点批量推理，动作 chunk 时序集成后经同步 RPC 回发，显式避免 ROS 依赖与指令堆积。

### 训练/评测协议（可复现性相关）

- 仿真：多任务混合训练 40k steps，batch 32；LIBERO 每任务 50 试次，RoboTwin 每任务 100 试次。
- 真机：每任务单任务微调 20k steps，20 试次。
- 仿真鱼眼数据生成：RoboTwin 广角腕相机垂直 FOV 150°、渲染 680×680、每任务 50 条成功轨迹；LIBERO 去掉第三人称视角只留单腕相机、渲染 700×700，共享同一组畸变参数。转 LeRobot 格式时做固定重映射网格，鱼眼强度 s = 1.8，ρ_src = tan(ρ·arctan s)/s，单位圆外填黑边，双线性插值后 area 插值缩到 224×224。

---

## 实验关键数据

三层实验：诊断验证（问题真实存在吗）→ 数据级验证（两个数据侧补丁有效吗）→ 模型级验证（整体系统赢吗）。

### 诊断层：两个 mismatch 确实存在

**策略侧退化（Table 1，成功率 %）**

| Model | LIBERO 标准视角 | LIBERO 腕鱼眼 | RoboTwin 标准视角 | RoboTwin 腕鱼眼 | 平均下降 |
|---|---|---|---|---|---|
| π0.5 | 96.3 | 92.2 | 82.0 | 59.4 | 13.4 |
| LingBot-VLA | 85.3 | 81.7 | 77.6 | 49.9 | 15.7 |
| Wall-X | 74.6 | 70.0 | 14.9 | 15.2 | 2.2 |

另有一个更强的对照：RoboTwin 上若换成**窄 FOV 纯腕透视相机**，π0.5 成功率暴跌到 13.1%；换成 UMI 式广角鱼眼可以捞回相当一部分上下文，但与标准视角仍有明显差距。这个梯度（窄腕 13.1 → 鱼眼 59.4 → 标准 82.0）比表格本身更有说服力，说明**鱼眼是「信息够但难解析」，而不是「信息不够」**。

**感知侧退化（Table 2）**：在 Where2Place / RefSpatial / ERQA / EmbSpatial 四个空间推理基准上，对比原图与鱼眼变换图，Qwen2.5VL-3B、Qwen3VL-4B、Embodied-R1-3B、RoboBrain2.5-4B、VLASER-2B 五个模型一致下降，平均绝对下降 4.0 分（相对 8.6%），单模型相对下降区间 4.5%–13.7%。

**轨迹侧不可执行性**：在 glue stick handover / drawer pulling / stapler placement 三个任务上审计 raw UMI 演示，MuJoCo 与真机双重 replay，观察到「轨迹语义正确但机器人执行偏离」，并将该偏离认定为任务失败的根因（Fig. 6）。**注意：这一节全是定性 + 曲线，论文没有给出「多大比例的 raw 轨迹不可执行」的统计数字**，尽管引言中用了 "a large fraction" 的措辞。

### 数据层：UMI-VQA 共训（Table 3，真机 20 试次/任务）

| 训练设置 | Stack Cubes | Stack Paper Cups | Stack Pen Holders | 总计 |
|---|---|---|---|---|
| Action-only π0.5 | 45.0% | 50.0% | 40.0% | 27/60 = 45.0% |
| π0.5 + 标准视角 VQA | 20.0% | 20.0% | 55.0% | 19/60 = 31.7% |
| π0.5 + UMI-VQA | 40.0% | 55.0% | 70.0% | **33/60 = 55.0%** |

最有意思的不是 UMI-VQA 赢，而是**标准视角 VQA 比不加 VQA 还差 13.3 个点**——作者的解释是共享 backbone 下的分布失配会把表征学习拽离 UMI 动作预测所需的视觉线索。若成立，这对整个 VLA 社区「随手掺点通用 VQA 共训」的惯例是个反直觉警告。

### 数据层：物理打分与部署成功率（Table 4，stapler placement，RealMan，各 50 条演示、20 试次）

| 子集 | 连续性 | 碰撞 | 保真度 | 总分 | GSR | OSR | PSR |
|---|---|---|---|---|---|---|---|
| 低分子集 | 100.00 | 94.69 | 39.35 | 35.50 | 0.55 | 0.00 | 0.00 |
| 高分子集 | 100.00 | 100.00 | 99.21 | 99.21 | 0.65 | 0.65 | 1.00 |

指标定义：GSR = 抓取成功率，OSR = 整体成功率，PSR = 抓后成功率 = OSR/GSR。这个分解设计得很好——它把「本体导致的执行失败」精确定位到抓后放置阶段，而两个子集 GSR 相近（0.55 vs 0.65）恰好说明差异不来自抓取能力而来自可执行性。

**跨本体重打分（Fig. 7 + Table 5）**：把在 RealMan 上选出的同一对子集拿到 R1Pro 与 ACone 上重新打分与部署：

| 训练数据 | RealMan (GSR/OSR/PSR) | ACone | R1Pro |
|---|---|---|---|
| 低分子集 | 0.55 / 0.00 / 0.00 | 0.60 / 0.00 / 0.00 | 0.80 / **0.80** / 1.00 |
| 高分子集 | 0.65 / 0.65 / 1.00 | 0.60 / 0.55 / 0.92 | 0.75 / 0.75 / 1.00 |

这是全文我认为最有信息量的一张表：**「低分数据」在 R1Pro 上完全可用（OSR 0.80，甚至高于高分子集的 0.75）**，直接证明数据质量不是内禀属性而是本体条件量，也就证成了 S(ξ,e) 这个函数签名本身。

### 模型层

**仿真（Table 6，同一批重采集演示）**

| Model | RoboTwin-UMI | LIBERO-UMI | Avg. |
|---|---|---|---|
| Wall-X | 0.152 | 0.700 | 0.426 |
| LingBot-VLA | 0.499 | 0.817 | 0.658 |
| π0.5 | 0.594 | 0.922 | 0.758 |
| **VISTA** | **0.683** | **0.943** | **0.813** |

**真机（Table 7，20 个任务 × 20 试次）**：LingBot-VLA 0.313，π0.5 0.528，VISTA 0.598（相对 π0.5 +7.0 点）。

### 消融（Table 8，RoboTwin-UMI）

| 变体 | 成功率 | 说明 |
|---|---|---|
| VISTA 完整 | 68.3 | — |
| w/o Stage 2，scratch expert | 52.4 | −15.9，Stage-2 学到的动作先验是最大单一贡献 |
| w/o Stage 2，用原始 π0.5 expert | 60.2 | −8.1，说明收益不只是「有个 expert」，而是这个 expert 被重新训过 |
| w/o 本体状态输入 | 61.9 | −6.4，本体与执行上下文对视觉观测有补充作用 |
| w/o delta action（改绝对动作） | 53.1 | −15.2，相对动作表示对「手持轨迹 → 机器人执行」的映射至关重要 |

注意力可视化（Fig. 10）为定性结果：在**非操作手**的腕相机视角下，π0.5 注意力弥散，VISTA 更集中在活动夹爪、被操作物与局部交互区。

### 关键发现

- **鱼眼是可解析性问题而非信息量问题**：窄腕 13.1% → 鱼眼 59.4% → 标准 82.0% 的梯度说明广角补回了上下文，剩下的差距来自畸变几何本身，因而「补数据做感知对齐」而不是「换相机」是对症的。
- **辅助 VQA 共训不是免费午餐**：标准视角 VQA 使成功率从 45.0% 掉到 31.7%。这意味着共训数据的**视觉分布**必须与动作数据对齐，否则共享 backbone 会被拉偏。
- **可执行性打分主要由 execution fidelity 承载**：Table 4 中两个子集的连续性都是满分 100，碰撞分 94.69 vs 100，真正拉开差距的是保真度 39.35 vs 99.21。我的判断是，论文标题里三维打分的实际判别力高度集中在一维上，另两维在这个实验里近乎无效。
- **数据质量是本体条件量**：同一批「废料」换台机器人 OSR 从 0.00 变 0.80。这条结论比 VISTA 的整体成功率提升更有普适价值。
- **delta action 与 Stage-2 expert 是模型侧的两根支柱**：各贡献约 15 点，远大于 state 输入的 6.4 点。

---

## 亮点与洞察

可复用的东西，按我认为的价值排序：

1. **S(ξ, e) 这个函数签名**。把「这条数据好不好」重定义为关于目标本体的条件量，并给出跨本体平均 S_cross 用于通才预训练、单本体 S 用于专才微调的两阶段阈值策略。这个抽象可以直接搬到任何 cross-embodiment 数据curation 场景，与打分公式的具体形式无关。
2. **乘积聚合而非加权和**。物理可行性是合取式约束（任一维崩掉就不可用），加权和会让高分维度补偿致命缺陷。Σw_i = 3 的归一化让默认全 1 时退化为几何平均的三次方形式，量纲仍在 [0,100]。这是个小而正确的建模选择。
3. **GSR / OSR / PSR 三元分解**。用 PSR = OSR/GSR 把「阶段性能力」与「本体执行性」解耦，使得「两组 GSR 相近但 PSR 0.00 vs 1.00」成为一个很干净的因果证据。任何做真机策略评测的工作都可以借这个分解，成本为零。
4. **软验证 vs 硬过滤**。连续分数 + 可调阈值 + 可调权重（微调时抬 w2 偏好无碰撞）比二值 filter 灵活得多，且允许同一份数据在不同阶段被重复利用。
5. **透视转鱼眼要用生成模型而非几何 warp 的论证**。当目标 FOV 超出原视锥时，几何变换在数学上就无法产生周边内容，这个论证本身是干净的，可迁移到任何跨相机模型的数据增广设计。
6. **知识隔离式两阶段训练**：Stage 1 用离散 token 让 backbone 学对齐，Stage 2 冻 backbone 训连续专家。消融显示重训 expert 比复用 π0.5 expert 多 8.1 点，说明这条 recipe 不是走过场。

---

## 局限与展望

### 作者承认的局限

几乎没有。**论文没有独立的 Limitations 小节**，结论段也未列举失败模式或适用边界。这本身是个信号。

### 读者应警惕的局限（按严重程度）

1. **数据完整性预检从未被定义**。摘要、§3.3 正文、Fig. 4 三处都提到「data-completeness pre-check」是流水线第一步，但全文（含附录）没有任何一处说明它检查什么、阈值多少、剔除比例多大。如果这一步实际剔除了大量样本，那么后续三维打分的效果评估是有偏的。这是复现该 pipeline 的第一个硬障碍。
2. **最小值聚合过于激进且未做敏感性分析**。s_tc、s_ef 都是「每时刻取位置/角度中较低者，再对全程取最小值」。这意味着**单个时刻的尖峰就能把整条轨迹判死**——对传感器毛刺极不鲁棒，且与「软验证」的宣称有张力（分数虽连续，但由极值决定，实际行为接近硬过滤）。论文未比较 min 与分位数/均值聚合的差异。
3. **「打分强预测部署成功」的证据实际上只有两个数据点**。摘要称 physical-validation scores are strongly predictive of deployment success，但支撑它的是单个任务（stapler placement）、单一本体选择、各 50 条演示、每组 20 试次的**两组对照**。没有多点相关性曲线，没有多任务复现，没有随机种子重复。20 试次下 OSR 的 95% 置信区间约 ±0.2，两组差距（0.00 vs 0.65）虽大到能扛住，但「strongly predictive」这种定量措辞需要的是相关系数，论文没给。
4. **三维打分中两维未被验证**。Table 4 中连续性对两组均为 100、碰撞分 94.69 vs 100，判别力几乎全部来自执行保真度。也就是说，**s_tc 与 s_sr 这两维在全文中没有任何实验证明其必要性**——缺少「只用 s_ef 筛选」的消融。若该消融结果相近，三维框架就退化为一维。
5. **低分/高分子集的构造存在混杂风险**。两个子集按分数选出，但论文未说明它们在任务阶段分布、运动幅度、演示者、采集时段上是否可比。低分数据也可能只是「动作幅度更大的一类演示」，其失败可归因于分布差异而非可执行性。理想对照是对同一批轨迹做可执行性投影后再训练。
6. **阈值全部未报告**。「宽松阈值」「严格阈值」贯穿方法与实验，但没有一个具体数值；d_col,min / d_col,max 也未给。这使得打分流水线在数值上不可复现（除非代码里有）。
7. **预训练算力不对等**。VISTA 从 π0.5 初始化，再吃 8M VQA + 100K 轨迹做继续预训练；Table 6/7 所称的「same data」指的是下游训练数据相同，**不包括这部分额外预训练**。因此 VISTA vs π0.5 的 5.5 / 7.0 点差距中，有多少来自方法、有多少来自额外的预训练算力与数据量，实验无法分离。这是主实验最实质的 confound。
8. **Wall-X 作为 baseline 在 RoboTwin 上基本是失效的**（标准视角 14.9，鱼眼 15.2，鱼眼反而更高）。一个在标准设置下就不工作的基线，其「平均下降 2.2」没有解释力，放进 Table 1 反而稀释了论证。
9. **感知退化实验用的是合成鱼眼**。Table 2 的鱼眼图由 s=1.8 的解析式重映射生成，不是真实鱼眼镜头成像；而 UMI-VQA 的 5M 补充部分是 FLUX 扩散编辑生成的。两者变换方式不同（这点降低了直接泄漏风险），但也意味着**训练侧鱼眼与评测侧鱼眼、与真实镜头，三者是三种不同分布**，「对齐」的对象究竟是哪一个并不清楚。
10. **UMI-VQA 的标注质量无任何量化**。3M 条由 VLM 生成后「轻量人工校验」，未报告抽检准确率、校验覆盖率、人工工时或错误类型分布。对一个宣称首创的数据集，这是明显的缺口。
11. **自碰撞检查显式排除场景物体**，因此不覆盖环境碰撞。在杂乱场景下，这一维的实用价值会显著缩水，论文未讨论。
12. **Table 3 的每格只有 20 试次**，单任务波动可达 ±11 个点；「标准视角 VQA 有害」这一反直觉结论主要由 Task 1/2 的 20% 撑起，而 Task 3 反而是 55%（高于 action-only 的 40%）。结论方向我倾向于相信，但强度被高估了。

### 可做的下一步

- 补「只用 s_ef 筛选」与「min vs 分位数聚合」两个消融，这两个实验成本极低，却直接决定该框架是三维还是一维、是软验证还是伪软验证。
- 把 S(ξ,e) 与部署成功率画成散点/相关曲线（多任务、多阈值段），用相关系数替代两组对照，才配得上 "strongly predictive"。
- 与其把低分轨迹丢弃，不如**把它们投影到目标本体的可行流形上再用**——保住数据量的同时消除不可执行性，这是这条线最自然的后续。
- 把 S(ξ,e) 从离线筛选变成训练时的**样本权重**或课程排序，避免阈值处的硬截断。
- 环境碰撞与接触力维度的补齐；以及把控制器带宽显式建模，而不是让它隐含在 e_replay 里。

---

## 相关工作与启发

| 轴 | UMI / FastUMI 系 | VISTA |
|---|---|---|
| 数据立场 | UMI 数据可被任意下游学习器直接消费 | 对大 VLA 不成立，需先做感知与物理两重转换 |
| 质量控制 | 硬过滤：违反关节限位/动力学可行性即丢弃（Chi et al. 2024; Wang et al. 2026） | 软验证：连续三维打分 + 可调阈值 + 本体条件化 |
| 鱼眼域应对 | FastUMI 用单目深度估计生成伪深度图 | 作者批评这只是加了一个新模态，没有把 backbone 的视觉 grounding 对齐到鱼眼域；VISTA 用同域 VQA 共训 |
| 本体适配 | 主要靠模型侧对齐动作分布（如 Zhang et al. 2025） | 主要靠数据侧筛选 + delta action 表示 |
| 评测 | 多为紧凑模仿学习策略（DP / ACT） | 大规模 VLA，含 20 个真机任务与三本体交叉部署 |

对我们自己工作的启发（我的判断，非论文主张）：

- `policy/real-world-policy-evaluation-2026-08.md` 那条线可以直接吸收 **GSR/OSR/PSR 分解**与**跨本体重打分**这两个协议设计，它们与具体方法解耦，只要有 replay 环境就能用。
- 若我们要复用其打分框架，**优先只实现 execution fidelity 一维**（MuJoCo replay + IK + 跟踪误差），因为这是唯一被实验证据支撑的一维，实现成本也最低；连续性一维可用更鲁棒的分位数聚合而非最小值。
- 「共训 VQA 必须与动作数据同视觉域，否则有害」这条如果在我们的设置里复现得出来，会是个独立可发表的观察。

---

## 评分

- **新颖性 3.5 / 5**：S(ξ,e) 的本体条件化数据质量观念是真的新且干净；但 UMI-VQA 本质是标准的「大 VLM 造数据 + 共训」配方，模型侧完全沿用 π0.5 + flow matching + 知识隔离，无架构贡献。
- **实验充分度 3 / 5**：三层实验的组织结构值得称赞，真机 20 任务 + 三本体交叉部署的工作量扎实；但核心论断（打分预测部署成功）只有两个数据点，三维打分中两维无消融，预训练算力与基线不对等。
- **写作质量 4 / 5**：问题拆解与动机论证是全文最强的部分，几何 warp 不可行的论证、软/硬验证的对比都写得清楚；扣分在于缺 Limitations、预检从未定义、多处阈值缺失。
- **复现/落地价值 3 / 5**：pipeline 依赖 MuJoCo + Mink + 各机器人 URDF，工程量不小，且论文未给关键阈值；但承诺开源 pipeline 与数据，若代码兑现则落地价值可上调到 4。
- **对当前研究的启发 4 / 5**：「数据质量是本体条件量」与「异域 VQA 共训有害」两条观察的外部性远超本文方法本身。

---

## 一句话行动建议

值得引用（作为 cross-embodiment 数据 curation 与手持数据可执行性问题的代表工作），谨慎复现（先只做 execution fidelity 一维 + 分位数聚合），**不要直接引用其「三维打分强预测部署成功」的结论强度**——证据只支撑到「执行保真度低的子集在特定本体上部署会失败」这一层。
